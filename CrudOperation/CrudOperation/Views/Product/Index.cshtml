@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<CrudOperation.Models.Product>
@* 
<h2>Product List</h2>
<a href="/Product/Create">Add New Product</a>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.ProductId</td>
                <td>@product.Name</td>
                <td>@product.Price</td>
                <td>@product.Quantity</td>
                <td>
                    <a href="/Product/Edit/@product.ProductId">Edit</a>
                    <a href="/Product/Delete/@product.ProductId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@
<div class="product-list-container">
    <a href="/Product/Create" class="add-product-button">Add New Product</a>

    <table class="table-style">
        <thead>
            <tr>
              @*   <th>ID</th> *@
                <th>Name</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in Model)
            {
                <tr>
                    @* <td>@product.ProductId</td> *@
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>@product.Quantity</td>
                    
                       @*  <a href="/Product/Edit/@product.ProductId">Edit</a>
                        <a href="/Product/Delete/@product.ProductId">Delete</a> *@

                    <td>
                        <button class="action-button edit-button" asp-route-id="@product.ProductId" onclick="location.href='/Product/Edit/@product.ProductId'">Edit</button>
                        <button class="action-button delete-button" asp-route-id="@product.ProductId" onclick="location.href='/Product/Delete/@product.ProductId'">Delete</button>
                    </td>

                    
                </tr>
            }
        </tbody>
    </table>
</div>

